export interface UserItemType {
  useStatus: string
  user: {
    userId: string
    email: string
    name: string
    grade: number
    classNum: number
    number: number
    profileImageUrl: string
  }
}
